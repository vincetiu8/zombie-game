name: Lint Code Base
on:
    pull_request: 
jobs:
    build:
        name: Lint code base
        runs-on: ubuntu-latest
        steps:
            -   name: Checkout Code
                uses: actions/checkout@v2
                
            -   name: Run Resharper
                uses: glassechidna/resharper-action@master
                with:
                    solution: zombie-game.sln